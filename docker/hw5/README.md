# hw5

## windows

```powershell
docker run --rm -it -v ${pwd}:/source twyair/safot-hw:5
```

## linux

```powershell
docker run --rm -it -v $(pwd):/source twyair/safot-hw:5
```

you may need to use `sudo`
